// Scale up on hover
export const scaleUpHover = {
  initial: { scale: 1 },
  hover: { scale: 1.05 },
};

// Lift up on hover
export const liftUpHover = {
  initial: { y: 0 },
  hover: { y: -5 },
};

// Rotate on hover
export const rotateHover = {
  initial: { rotate: 0 },
  hover: { rotate: 5 },
};

// Glow effect on hover
export const glowHover = {
  initial: { boxShadow: "0 0 0 rgba(0,0,0,0)" },
  hover: { boxShadow: "0 0 20px rgba(0,0,0,0.2)" },
};
